from django.apps import AppConfig

class FrontdeskstaffConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.frontdeskstaff'  # 🌟 Make sure 'apps.' is explicitly here!